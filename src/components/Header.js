import React from 'react';

const links = [
    'home',
    'about',
    'blog',
];

const Header = () => (
    <div>
        This is the header div.
    </div>
);

Header.displayName = 'Header';
export default Header;